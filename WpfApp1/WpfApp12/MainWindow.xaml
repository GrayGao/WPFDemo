<Window x:Class="WpfApp12.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp12"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <!--<Window.Resources>
        <SolidColorBrush x:Key="SolidColor" Color="Red"/>
    </Window.Resources>-->
    <Grid>
        <StackPanel>
            <Button Margin="10" Content="Update" Click="Button_Click"
                    Style="{StaticResource DefaultButtionStyle}"/>
            <Button Margin="10" Height="40" BorderThickness="5"
                    BorderBrush="{StaticResource SolidColor}"
                    Content="Button1"/>
            <Button Margin="10" Height="40" BorderThickness="5"
                    BorderBrush="{DynamicResource SolidColor}"
                    Content="Button1"/>
        </StackPanel>
    </Grid>
</Window>
